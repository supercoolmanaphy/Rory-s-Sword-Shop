// Admin credentials for the shop.
// To change the password, compute the SHA-256 hex digest of your chosen password
// (e.g. using https://emn178.github.io/online-tools/sha256.html) and replace
// ADMIN_PASSWORD_HASH below. Use a strong, unique password.
// Note: this file is served publicly; keep it free of plaintext secrets.
export const ADMIN_EMAIL = 'supercoolmanaphy@gmail.com';
export const ADMIN_PASSWORD_HASH = 'REPLACE_WITH_SHA256_OF_YOUR_PASSWORD';
